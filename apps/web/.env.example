# Required — external PocketBase host (no trailing slash, no /api suffix).
# Auth example: ${VITE_POCKETBASE_URL}/api/collections/users/auth-with-password
VITE_POCKETBASE_URL=https://your-pocketbase-url.up.railway.app
